package level;

/**
 * Enum that defines the difficulty of a level
 *
 * @author Jef
 * @since 1.0
 * @version 1.0
 */
public enum Difficulty {
    EASY,
    NORMAL,
    HARD,
    VERY_HARD,
    INSANE
}
